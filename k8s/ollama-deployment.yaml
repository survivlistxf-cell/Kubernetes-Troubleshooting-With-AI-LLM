apiVersion: apps/v1
kind: Deployment
metadata:
  name: ollama
spec:
  replicas: 1
  # Recreate: nu pornim un al doilea pod care ar concura pe RAM-ul nodului
  # dedicat in timpul unui rollout.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: ollama
  template:
    metadata:
      labels:
        app: ollama
    spec:
      # --- Pin DOAR pe VM-ul dedicat Ollama ---
      # Nodul nou trebuie etichetat:   kubectl label node <nume-nod> workload=ollama
      # si "murdarit" (taint) ca sa NU primeasca alte pod-uri:
      #   kubectl taint node <nume-nod> dedicated=ollama:NoSchedule
      nodeSelector:
        workload: ollama
      tolerations:
        - key: "dedicated"
          operator: "Equal"
          value: "ollama"
          effect: "NoSchedule"
      containers:
        - name: ollama
          image: ollama/ollama:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 11434
              name: http
          env:
            - name: OLLAMA_HOST
              value: "0.0.0.0:11434"
            # tine modelul incarcat in RAM intre request-uri (evita reload lent)
            - name: OLLAMA_KEEP_ALIVE
              value: "24h"
            # Doar embeddings (nomic-embed-text) => un singur model incarcat.
            - name: OLLAMA_MAX_LOADED_MODELS
              value: "1"
          # Embeddings cu nomic-embed-text (~270MB) sunt usoare si merg pe CPU,
          # deci resursele sunt mult reduse fata de inferenta LLM.
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: "2"
              memory: 4Gi
          volumeMounts:
            - name: ollama-models
              mountPath: /root/.ollama
          # Dupa pornirea serverului, descarca DOAR modelul de embeddings.
          # Chat-ul a migrat pe gpt-oss extern; aici nu mai e nevoie de modele LLM.
          # Ruleaza o singura data efectiv — daca exista deja pe disc, pull e instant.
          lifecycle:
            postStart:
              exec:
                command:
                  - /bin/sh
                  - -c
                  - "until ollama list >/dev/null 2>&1; do sleep 2; done; ollama pull nomic-embed-text"
          readinessProbe:
            httpGet:
              path: /
              port: 11434
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /
              port: 11434
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 10
      volumes:
        # Pod-ul e fixat pe un singur nod, deci hostPath pastreaza modelele
        # (~5-6GB) intre restart-uri, fara re-download. Alternativ: un PVC Cinder.
        - name: ollama-models
          hostPath:
            path: /var/lib/ollama
            type: DirectoryOrCreate
